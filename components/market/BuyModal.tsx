/**
 * components/market/BuyModal.tsx
 *
 * Alias kept for import stability: the purchase box lives in
 * CheckoutModal.tsx (single Buy Now → popup) since the Courtyard-style
 * checkout landed. Both names take the same listing prop.
 */
export { CheckoutButton as BuyModal } from "./CheckoutModal";
export type { BuyModalProps } from "./CheckoutModal";
